@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    int i = 1;
}
@model List<EmployeeProjectViewModel>

@* <h2>Employees with Projects and Hours</h2>

@foreach (var employee in Model)
{
    <div>
       
        <ul>
            @foreach (var project in employee.Projects)
            {
                 <h4>@employee.EmployeeFullName</h4>
                <li>@project.ProjectName - Hours: 
                <span style="color: @(project.Hours < 5 ? "red" : "green")">@project.Hours</span> </li>
            }
        </ul>
    </div> *@
}

<div class="container">
    <h1 class="con">Employees with Projects and Hours</h1>



    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">EmployeeFullName</th>
                <th scope="col">ProjectName</th>
                <th scope="col">project.Hours</th>
                
            </tr>
        </thead>



        <tbody>

            @foreach (var employee in Model)
{

            @foreach (var project in employee.Projects)
            {
                <tr>
                        <td>@(i++)</td>
                    <td> @employee.EmployeeFullName</td>
                    <td> @project.ProjectName</td>
                    <td style="font-size:25px"> <span style="color : @(project.Hours < 5 ? "red" : "green")">@project.Hours</span>  </td>
                   
                </tr>

            }}
        </tbody>

    </table>
